import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF227C9D);     // ฟ้าเข้ม
  static const secondary = Color(0xFF17C3B2);   // ฟ้าเขียว
  static const accent = Color(0xFFFFCB77);      // เหลืองส้ม
  static const background = Color(0xFFF6F6F6);  // สีพื้นหลัง
  static const text = Color(0xFF333333);        // สีข้อความเข้ม
  static const error = Color(0xFFD32F2F);        // สี error
}
